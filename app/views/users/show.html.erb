<h1><%= @user.name %></h1>
<div>
  <%=image_tag user_avatar(@user), alt: "[foto]" %>
</div>
<h2>@<%= @user.username %></h2>

<a href="#" class="button-green">Ask question</a>

<% @questions.each do |question| %>
  <%= render partial: 'layouts/questions/question', object: question %>
<% end %>
